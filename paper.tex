\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
% Force Arabic numbering for sections and subsections as requested
\renewcommand\thesection{\arabic{section}}
\renewcommand\thesubsection{\thesection.\arabic{subsection}}
\renewcommand\thesubsubsection{\thesubsection.\arabic{subsubsection}}

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{orcidlink}
\hypersetup{hidelinks}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{OmniGen: Evaluation Against SOTA Image Generation Models}

\author{\IEEEauthorblockN{Huy Ngoc Chau \orcidlink{0009-0004-3554-059X}}
\IEEEauthorblockA{\textit{University of Science} \\
\textit{Viet Nam National University}\\
Ho Chi Minh City, Vietnam \\
25C1100828@student.hcmus.edu.vn \\}
\and
\IEEEauthorblockN{Minh Thang Hoang \orcidlink{0009-0004-3554-059X}}
\IEEEauthorblockA{\textit{University of Science} \\
\textit{Viet Nam National University}\\
Ho Chi Minh City, Vietnam \\
25C1102003@student.hcmus.edu.vn \\}
}

\maketitle

\begin{abstract}
OmniGen is a unified image generation model designed to handle multiple image generation tasks within a single framework. It is among the first approaches to jointly support diverse tasks, while achieving performance comparable to task-specific state-of-the-art (SOTA) models.

Motivated by these claims, we conduct a systematic evaluation of OmniGen against recent SOTA models across three representative tasks: text-to-image generation, instruction-based image editing, and style transfer. Our evaluation considers both task-specific performance metrics and computational efficiency.

Experimental results, consistent with those reported in the original work, indicate that OmniGen achieves competitive performance in general image generation tasks. However, it shows limitations in tasks requiring precise control and fine-grained manipulation, where specialized models still outperform the unified approach.

These findings highlight the trade-offs between generality and task-specific performance, suggesting that while unified models offer flexibility and simplicity, specialized architectures remain essential for achieving optimal results in individual tasks.
\end{abstract}

\begin{IEEEkeywords}
OmniGen, Reproducibility, Unified Image Generation, Diffusion Transformer, Multimodal Generation
\end{IEEEkeywords}

\section{Introduction}
\label{sec:introduction}

% Recent diffusion models deliver high-quality text-to-image results, but practical workflows are still fragmented across task-specific add-ons and custom preprocessing. OmniGen proposes a different paradigm: one model, one instruction interface, many tasks \cite{omnigen2024}. This is important for real applications where users need editing, control, and subject consistency in the same session.

% This paper targets reproducibility. Rather than introducing a new model, we formalize how to reproduce OmniGen behavior in a transparent and reusable manner. Our scope is inference-oriented reproduction with benchmark-aligned evaluation settings.

% Our contributions are:
% \begin{enumerate}
%     \item A concise technical summary of OmniGen components that matter most for reproduction: multimodal tokenization, mixed attention, and rectified-flow inference.
%     \item A step-by-step reproduction protocol covering environment setup, prompt formatting, guidance scales, and multi-task evaluation.
%     \item A benchmark-oriented result template that aligns reproduced observations with reported OmniGen outcomes on GenEval, image editing, subject-driven generation, and visual conditions.
% \end{enumerate}

% The rest of this paper is organized as follows. Section \ref{sec:related_work} reviews related foundations. Section \ref{sec:method} describes OmniGen and the reproduction protocol. Section \ref{sec:experiments} presents the evaluation setup and reproduction-oriented results. Section \ref{sec:conclusion} concludes with limitations and future extensions.

\section{Related Work}
\label{sec:related_work}

% Latent diffusion models such as SDXL are strong generators but usually depend on separate encoders and optional modules for different conditions \cite{rombach2022high}. More recent large diffusion transformers improve quality at scale, e.g., SD3 \cite{sd3}. In parallel, controllable generation methods such as ControlNet extend base models with dedicated condition branches \cite{controlnet}.

% OmniGen is distinct because it unifies conditions and tasks under a single multimodal instruction interface and a compact architecture \cite{omnigen2024}. For evaluation, prior work uses object-centric text-to-image alignment benchmarks (GenEval \cite{geneval}), editing benchmarks (EMU-Edit \cite{emuedit}), and subject-driven benchmarks (DreamBench \cite{dreambench}). We adopt these benchmarks in our reproduction protocol for comparability.

% Hi, this is a simple test.

\section{Method}
\label{sec:method}

\subsection{OmniGen Architecture Summary}
% OmniGen consists of two main modules: a frozen VAE and a transformer backbone initialized from Phi-3 \cite{phi3,omnigen2024}. Text is tokenized with the language tokenizer, while images are mapped to latent patches and inserted into the token stream with special image boundary tokens. This design removes separate condition encoders and keeps the pipeline simple.

% OmniGen uses mixed attention: causal attention over the full sequence, with bidirectional attention inside each image token block. This improves latent denoising quality versus plain causal masking for visual tokens.

\subsection{Training Objective}
% The model follows rectified flow training \cite{omnigen2024,sd3}. At diffusion time $t$, noisy latent is:
% \begin{equation}
% \mathbf{x}_t = t\mathbf{x} + (1-t)\boldsymbol{\epsilon}, \quad \boldsymbol{\epsilon} \sim \mathcal{N}(0,1)
% \end{equation}
% and velocity prediction loss is:
% \begin{equation}
% \mathcal{L} = \mathbb{E}\left[\left\|(\mathbf{x}-\boldsymbol{\epsilon}) - v_\theta(\mathbf{x}_t,t,c)\right\|_2^2\right].
% \end{equation}
% For editing tasks, OmniGen introduces region-weighted loss to avoid shortcut copying from input image to output image \cite{omnigen2024}.

\subsection{Reproduction Protocol}
% We reproduce OmniGen behavior in four stages:
% \begin{enumerate}
%     \item \textbf{Environment and Assets}: use official checkpoint, official tokenizer, and SDXL VAE configuration.
%     \item \textbf{Prompt Standardization}: convert all tasks into a single interleaved text-image instruction format.
%     \item \textbf{Inference Settings}: 50 sampling steps, classifier-free guidance, text guidance scale $2.5$, image guidance scale $1.6$ (editing: $2.0$), matching reported setup \cite{omnigen2024}.
%     \item \textbf{Task Groups}: text-to-image, instruction-based image editing, subject-driven generation, and visual-condition generation.
% \end{enumerate}

% The core reproduction principle is to preserve the original instruction format. Instead of switching models per task, we only alter prompt content and input images.

\section{Experiments}
\label{sec:experiments}

\subsection{Experimental Setup}
% We follow benchmark settings from prior studies to keep evaluation comparable. For text-to-image, we use GenEval \cite{geneval}. For editing quality, we follow EMU-Edit \cite{emuedit} with CLIP-based instruction and image similarity metrics \cite{radford2021learning}. For subject-driven generation, we use DreamBench \cite{dreambench}. For visual conditions, we compare against ControlNet-style settings on segmentation, edge, and depth conditions \cite{controlnet}.

\subsection{Evaluation Metrics}
% We report:
% \begin{itemize}
%     \item \textbf{GenEval overall score} for object-centric text-image alignment.
%     \item \textbf{CLIP-T} and \textbf{CLIP-I} for editing and subject-driven tasks.
%     \item \textbf{Condition similarity metrics} (mIoU/F1/SSIM/RMSE) for controllable generation.
% \end{itemize}

\subsection{Results}
% Table \ref{tab1} summarizes key reference results reported by OmniGen and used as reproduction targets. These numbers show that a unified model can remain competitive with larger or task-specialized systems.

% \begin{table}[htbp]
% \caption{Representative Results Reported for OmniGen and Used as Reproduction Targets}
% \begin{center}
% \begin{tabular}{|c|c|c|}
% \hline
% \textbf{Benchmark} & \textbf{Metric} & \textbf{OmniGen Value}\\
% \hline
% GenEval & Overall & 0.70 \\
% \hline
% EMU-Edit & CLIP-T / CLIP-I & 0.231 / 0.829 \\
% \hline
% DreamBench & CLIP-T / CLIP-I & 0.315 / 0.801 \\
% \hline
% Control Condition & Seg. mIoU & 40.06 \\
% \hline
% \end{tabular}
% \label{tab1}
% \end{center}
% \end{table}

% In qualitative reproduction, the unified prompting format consistently enables:
% \begin{itemize}
%     \item end-to-end image editing without extra plugin models,
%     \item subject transfer from one or multiple reference images,
%     \item visual-condition following from plain instruction plus reference image.
% \end{itemize}

\subsection{Discussion}
% The reproduction confirms the practical value of OmniGen's design: one model can replace multi-model orchestration in many cases. However, two limitations remain visible and should be stated for reproducibility fairness:
% \begin{itemize}
%     \item text rendering is still imperfect,
%     \item fine-grained anatomy and unseen condition maps can fail.
% \end{itemize}
% These observations are consistent with the original limitations report \cite{omnigen2024}.

\section{Conclusion}
\label{sec:conclusion}
% This paper provides a reproducible write-up for OmniGen-based image generation. We distilled the architecture, training/inference choices, and benchmark settings required to replicate the behavior of a unified image generator. The study indicates that OmniGen offers a strong trade-off between simplicity and capability across heterogeneous tasks. Future work includes full training reproduction on the original X2I scale, stronger text rendering, and process-level supervision for step-by-step image generation.

% Keep bibliography non-empty even when all in-text citations are commented out.
\nocite{omnigen2024}

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
